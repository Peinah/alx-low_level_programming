#include"main.h"
#include <stdio.h>
/**
 * main - outputs name
 * @argc:contains number  arguments supplied to program
 * @argv:argument supplied to program
 * Return:returns 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
		printf("%s\n", *argv);

			return (0);
}
