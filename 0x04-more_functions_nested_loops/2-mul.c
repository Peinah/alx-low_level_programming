#include "main.h"

/**
 * mul - mutiplies two integers
 * @a : first integer to multiply
 * @b : second integer
 * Return: product of integers
 */
int mul(int a, int b);
{
	int result;

	result = a * b;

	return (result);
}
